Problem:
    Read a file named numbers.txt containing N integers
    (separated by newlines). Store them in an integer array,
    sort them in ascending order using Bubble Sort or Selection
    Sort, and write the sorted list to sorted.txt
