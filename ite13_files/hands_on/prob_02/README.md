Problem:
    Read a file containing a 3x3 integer matrix (formatted as rows of numbers).
    Load this into a 2D array. Compute the transpose of the matrix and append
    the result to a file named matrix_log.txt
