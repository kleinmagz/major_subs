Problem:
    Assume you have two files, setA.txt and setB.txt, each
    containing 5 distinct integers. Read both files
    into two separate arrays. Create a third array that
    contains all integers from both files, remove any 
    duplicates, and write the final unique set to merged.txt