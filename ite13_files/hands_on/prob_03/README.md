Problem:
    Create a program that reads from scores.txt, which
    contains 10 lines. Each line has a student ID and a 
    score (e.g. 101 85). Store these in two parallel arrays
    (or one 2D array). Calculate the class average and write
    ID's of all students who scored above average to passers.txt
